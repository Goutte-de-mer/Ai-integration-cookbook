/**
 * Estimates token count and cost for a given input text and model.
 */

export default function CostEstimator() {
  return (
    <div className="rounded-lg border p-4">
      <h3 className="mb-2 font-semibold">Token &amp; Cost Estimator</h3>
      {/* TODO: textarea input, model selector, token count display, cost calc */}
    </div>
  );
}
